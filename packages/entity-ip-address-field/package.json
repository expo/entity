{
  "name": "@expo/entity-ip-address-field",
  "version": "0.34.0",
  "description": "IP address EntityField definitions for @expo/entity",
  "files": [
    "build",
    "src"
  ],
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "scripts": {
    "tsc": "tsc",
    "clean": "rm -rf build coverage coverage-integration",
    "lint": "eslint src",
    "lint-fix": "eslint src --fix",
    "test": "jest --rootDir . --config ../../resources/jest.config.js",
    "barrelsby": "barrelsby --directory src --location top --exclude tests__ --singleQuotes --delete"
  },
  "engines": {
    "node": ">=16"
  },
  "keywords": [
    "entity"
  ],
  "author": "Expo",
  "license": "MIT",
  "dependencies": {
    "ip-address": "^8.1.0"
  },
  "devDependencies": {
    "@expo/entity": "^0.34.0",
    "@types/jsbn": "^1.2.29"
  },
  "peerDependencies": {
    "@expo/entity": "*"
  }
}
